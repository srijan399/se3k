{
  "name": "se3k",
  "version": "1.0.0",
  "private": true,
  "description": "SE3K. Root helpers to install and run every package (mcp-server, slack-bot, web).",
  "scripts": {
    "install": "pnpm -C se3k/mcp-server install && pnpm -C se3k/slack-bot install && pnpm -C web install",
    "build": "pnpm -C se3k/mcp-server build",
    "setup": "pnpm run install && pnpm run build",
    "dev:bot": "pnpm -C se3k/slack-bot dev",
    "dev:web": "pnpm -C web dev",
    "build:web": "pnpm -C web build"
  }
}
