{
  "name": "se3k",
  "version": "1.0.0",
  "private": true,
  "description": "SE3K. Root helpers to install and run every package (mcp-server, slack-bot, web).",
  "scripts": {
    "install": "pnpm -C engine/mcp-server install && pnpm -C engine/slack-bot install && pnpm -C web install",
    "build:mcp": "pnpm -C engine/mcp-server build",
    "setup": "pnpm run install && pnpm run build:mcp",
    "dev:bot": "pnpm -C engine/slack-bot dev",
    "dev:web": "pnpm -C web dev",
    "dev:mcp": "pnpm -C engine/mcp-server dev",
    "build:web": "pnpm -C web build"
  }
}
